{
  "name": "effectengine",
  "private": true,
  "scripts": {
    "dev": "watchexec -r -w src -e rs --shell=none -- wasm-pack build --dev",
    "build": "pnpm build:cli && pnpm build:web",
    "build:cli": "cargo build -r",
    "build:web": "wasm-pack build"
  }
}
